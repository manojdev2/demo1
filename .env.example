# ============================================
# Anentaa  Environment Variables
# ============================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control - it contains sensitive information
# ============================================

# ============================================
# DATABASE CONFIGURATION
# ============================================
# MongoDB connection string
# Format: mongodb://username:password@host:port/database?authSource=admin
# For local MongoDB: mongodb://localhost:27017/Anentaa 
DATABASE_URL=mongodb://admin:password123@localhost:27017/Anentaa ?authSource=admin

# ============================================
# AUTHENTICATION CONFIGURATION
# ============================================
# Secret key for NextAuth.js session encryption
# Generate a secure random string using one of these methods:
#   - npm exec auth secret
#   - npx auth secret
#   - openssl rand -base64 33
# This is REQUIRED for authentication to work
AUTH_SECRET=your-auth-secret-here-generate-using-npm-exec-auth-secret

# Alternative: NEXTAUTH_SECRET (used as fallback if AUTH_SECRET is not set)
# NEXTAUTH_SECRET=your-auth-secret-here

# Base URL of your application
# For local development: http://localhost:3000
# For production: https://yourdomain.com
NEXTAUTH_URL=http://localhost:3000

# Trust host configuration (usually same as NEXTAUTH_URL)
# Set to true or the URL for production deployments
AUTH_TRUST_HOST=http://localhost:3000

# ============================================
# INITIAL USER CONFIGURATION (for seeding)
# ============================================
# These credentials will be used to create the initial admin user
# Change these before first deployment for security
USER_EMAIL=admin@example.com
USER_PASSWORD=password123

# ============================================
# OPENAI API CONFIGURATION
# ============================================
# Your OpenAI API key for AI features (resume review, job matching, cover letters)
# Get your API key from: https://platform.openai.com/api-keys
# This is REQUIRED for AI features to work
OPENAI_API_KEY=sk-your-openai-api-key-here

# ============================================
# STRIPE CONFIGURATION (Optional - for billing/subscriptions)
# ============================================
# Only required if you want to enable Stripe payment features
# Get your keys from: https://dashboard.stripe.com/apikeys

# Stripe Secret Key (server-side)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here

# Stripe Publishable Key (client-side)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here

# Stripe Webhook Secret (for webhook verification)
# Get this from: https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Stripe Price IDs for subscription plans
# Create products and prices in Stripe Dashboard, then add the price IDs here
STRIPE_PRICE_ID_FREE=
STRIPE_PRICE_ID_FRESHERS=price_your_freshers_plan_price_id
STRIPE_PRICE_ID_EXPERIENCE=price_your_experience_plan_price_id

# ============================================
# NODE ENVIRONMENT
# ============================================
# Set to 'production' for production builds, 'development' for local development
NODE_ENV=development

# ============================================
# NOTES
# ============================================
# 1. After creating .env from this file, make sure to:
#    - Generate a secure AUTH_SECRET
#    - Add your OpenAI API key
#    - Update database credentials if using external MongoDB
#    - Change default user credentials (USER_EMAIL, USER_PASSWORD)
# 3. Stripe configuration is optional - only needed if you want payment features
#
# 4. Never commit .env file to version control - it's already in .gitignore
